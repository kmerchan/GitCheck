#!/bin/env bash
# script to install dependencies and run GitCheck program
python -m pip install gitpython
python -m pip install gitdb
python -m pip install requests
# TODO: run our program which calls searching.py, github.py, and checker.py
